
const styles = {
  root: {

    // maybe the bottom of the toolbar should not have a shadow,
    // to signify it is part of the search result
    // boxShadow: 'inset 1px -3px 7px -7px rgb(0, 0, 0)',

    background: 'transparent',
    padding: '4px 20px 0px 20px',
  },
  toolbar: {
    backgroundColor: 'transparent'
  }
};

export default styles;
