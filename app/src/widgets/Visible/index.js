'use strict'

const Visible = ({ value }) => {
  // TODO add some code to handle the animation, and completely remove the
  // hidden element from the DOM once it is finished
  return null
}

export default Visible
